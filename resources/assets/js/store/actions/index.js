// user actions
import {
  setAccessToken,
  unsetAccessToken,
  setUserAndAccessToken,
  unsetUserAndAccessToken,
  setUser,
  unsetUser,

} from './user';

const actions = {
  // user actions
  setAccessToken,
  unsetAccessToken,
  setUserAndAccessToken,
  unsetUserAndAccessToken,
  setUser,
  unsetUser,
};


export default actions;