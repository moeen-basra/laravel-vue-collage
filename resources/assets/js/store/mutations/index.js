// user mutations
import {
  setAccessToken,
  unsetAccessToken,
  setUserAndAccessToken,
  unsetUserAndAccessToken,
  setUser,
  unsetUser,
} from './user';

const mutations = {

  // user mutations
  setAccessToken,
  unsetAccessToken,
  setUserAndAccessToken,
  unsetUserAndAccessToken,
  setUser,
  unsetUser,
};

export default mutations;